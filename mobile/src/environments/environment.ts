// Pointed at production (servioapp.ro). NOTE: update-local-ip.sh will overwrite
// this back to the local LAN IP — re-apply this URL after running that script.
export const environment = {
  production: false,
  apiUrl: 'https://servioapp.ro/api'
};
