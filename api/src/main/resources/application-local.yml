# Local development profile
# Start with: source local.env && mvn spring-boot:run -Dspring-boot.run.profiles=local

spring:
  jpa:
    show-sql: false

application:
  base-url: http://192.168.1.136:4200

cors:
  allowed-origins: http://192.168.1.136:4200,http://localhost:3000,http://localhost,capacitor://localhost,*

jwt:
  secret: phpauf1PhcHOqocdXY9O0Z6Bjn3jrsGu7kWG2yEqMbNkbPZxM0RFaFEG8NAReW6+

# S3 credentials and endpoint come from environment (see local.env)
# Defaults in application.yml point at GCS via the S3 XML API.

# Netopia sandbox (local testing)
# Using ngrok for callbacks - start with: docker compose -f docker-compose.infra.yml up ngrok -d
netopia:
  api-key: GMaWOObfdYQBlUWcuZFVnEqw-5DOTm6s7iOz5FFbdh0TYH23CJPEeKFx284g
  pos-signature: 37ZT-B3X1-VZOC-GOIK-2RXP
  base-url: https://secure.sandbox.netopia-payments.com
  notify-url: https://overprominently-hemodynamic-veronica.ngrok-free.dev/api/payments/netopia/notify
  redirect-url: http://192.168.1.136:4200/payment/confirmed

logging:
  level:
    com.servio.event: DEBUG
