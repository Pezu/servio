# Local development profile
# Start with: mvn spring-boot:run -Dspring-boot.run.profiles=local

spring:
  kafka:
    bootstrap-servers: localhost:9092

# Backend services
services:
  event-api:
    url: http://localhost:8081
  order-service:
    url: http://localhost:8082

# JWT secret (must match event-api)
jwt:
  secret: phpauf1PhcHOqocdXY9O0Z6Bjn3jrsGu7kWG2yEqMbNkbPZxM0RFaFEG8NAReW6+

# CORS configuration
cors:
  allowed-origins: http://localhost:4200,http://localhost:3000,http://192.168.1.213:4200,http://localhost,capacitor://localhost,*

logging:
  level:
    com.servio.gateway: DEBUG
