# Local development profile
# Start with: mvn spring-boot:run -Dspring-boot.run.profiles=local

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# Event API service URL
services:
  event-api:
    url: http://localhost:8081

# Netopia sandbox (local testing)
# IMPORTANT: For IPN callback to work locally, use ngrok:
#   1. Run: ngrok http 8080
#   2. Set NGROK_URL environment variable to your ngrok URL
#   3. Restart the order service
netopia:
  api-key: GMaWOObfdYQBlUWcuZFVnEqw-5DOTm6s7iOz5FFbdh0TYH23CJPEeKFx284g
  pos-signature: 37ZT-B3X1-VZOC-GOIK-2RXP
  base-url: https://secure.sandbox.netopia-payments.com
  notify-url: https://overprominently-hemodynamic-veronica.ngrok-free.dev/api/payments/netopia/notify
  redirect-url: http://192.168.0.249:4200/payment/confirmed

logging:
  level:
    com.servio.order: DEBUG
