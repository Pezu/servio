# Local development configuration
# Source this file before starting services: source local.env

export LOCAL_IP=192.168.0.249
export LOCAL_PORT_GATEWAY=8080
export LOCAL_PORT_FRONTEND=4200

# Ngrok for Netopia callbacks (get your token from https://dashboard.ngrok.com/)
export NGROK_AUTHTOKEN=3BsdCBz0NA4taSTVzAFaiCIse3K_3Dwx5h8qnZZjj6TTdGVUf

# Ngrok URL for Netopia callbacks
export NETOPIA_NOTIFY_URL=https://overprominently-hemodynamic-veronica.ngrok-free.dev/api/payments/netopia/notify

# GCS (S3-compatible) credentials live in the gitignored .env.local.secrets file.
# Run `source .env.local.secrets` after sourcing this file.
